import React from "react";
import Header from "./components/Header";
import styles from "./hero.module.scss";

const Hero = () => {
  return (
    <div>
      <Header />
    </div>
  );
};

export default Hero;
